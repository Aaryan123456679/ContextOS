[pytest]
asyncio_mode = auto
testpaths = tests/backend
markers =
    e2e: end-to-end tests requiring all external services
    slow: tests that take > 10s
    llm: tests that make real LLM API calls (cost money)
